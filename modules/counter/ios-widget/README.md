# iOS widget source of truth

This directory is the canonical source for widget extension files.

During prebuild, the config plugin will copy these files into generated `ios/`.

Do not treat `ios/CounterWidget` as source of truth.
